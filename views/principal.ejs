<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INSTITUTO ITALY</title>
    <style>
        .main-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 800px; /* Hacer el contenedor principal más grande */
            margin: 0 auto; /* Centrar el contenedor en la página */
            padding: 20px; /* Añadir un espacio interno al contenedor principal */
        }
        .title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
            text-align: center; /* Centrar el texto del título */
        }
        .button-group {
            display: flex;
            justify-content: center;
            gap: 40px; /* Aumentar el espacio entre los contenedores */
        }
        .button-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            border: 2px solid #007bff; /* Añadir un borde para mostrar el contenedor */
            padding: 20px; /* Añadir un espacio interno para el contenedor */
            border-radius: 10px; /* Añadir esquinas redondeadas */
        }
        .button {
            padding: 15px 30px; /* Aumentar el tamaño del botón */
            border: none;
            border-radius: 8px; /* Aumentar el radio del borde del botón */
            background-color: #007bff;
            color: #fff;
            font-size: 18px; /* Aumentar el tamaño de la fuente del botón */
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .button:hover {
            background-color: #0056b3;
        }
        .button-image {
            width: 100px; /* Ajustar el tamaño de la imagen */
            height: auto;
            margin-bottom: 20px; /* Aumentar el espacio entre la imagen y el botón */
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="title">INSTITUTO ITALY</div>
        <div class="button-group">
            <div class="button-container">
                <img src="/images/gestion.png" alt="Imagen 1" class="button-image">
                <button class="button" onclick="redirectToLista()">Gestión Alumnos</button>
            </div>
            <div class="button-container">
                <img src="/images/asistencia.png" alt="Imagen 2" class="button-image">
                <button class="button" onclick="redirectToAsistencia()">Asistencia Alumnos</button>
            </div>
        </div>
    </div>

    <script>
        function redirectToLista() {
            window.location.href = "/lista";
        }
    </script>
    <script>
        function redirectToAsistencia() {
            window.location.href = "/asi";
        }
    </script>
</body>
</html>
